---
import { POSTS_PATH } from '@/constants';
import RoundedLink from './RoundedLink.astro';

interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<RoundedLink href={`${POSTS_PATH}?tag=${tag}`}>{tag}</RoundedLink>
